##Monodromy


1. Top spaces
   
Let (X,x_0) be a pointed connected good topological space.
Covering map <-> pi_1(X,x_0) - sets

Let lambda be a ring, discrete topology.

Then lambda-local system on X <-> pi_1(X,x_0) f.g. lambda modules.

One can think interchangably sheaf on X and espace etale over X.
With that identification local system is a covering map s.t. the fibers have module structure that sort of varies continously along fibers.
the difference between vector bundle is that the fibers should be imagined disconnected and discrete i.e. like a covering map.

**The point is that each fiber Y_x of a covering map Y->X is a discrete set where pi(X,x) naturally acts on, since one can lift loops uniquely
along covering maps.


example) : S^1 U S^1 -> S^1 : trivial Z/2Z rank 1 loc system.
 x U x^2  S^1 U S^1 -> S^1 : Z/3Z loc sys corresponding to non trivial Z->(Z/3Z)* .

Note : rank n lambda loc sys <-> GLn(lambda) torsors <-> H^1_et(X,GLn(lambda)) <-> pi(X)->GLn(lambda) <-> continous map pi_et(X)->Gln(lambda)

Theorem if f : X->Y is a fiber bundle Rf^i_*lambda is a lambda local system. (fibers, thus its cohomology groups are locally constant) 

2. schemes + etale topology

Let (X,x_0) be a normal connected scheme.

pi_et(X,x_0) is defined as the automorphism group of the fiber functor F_x_0 : Fet/X->Sets 

"relation between different x_1"?

Still by construction pi_et(X,x_0) acts on Y_x_0.

finite etale covers Y->X <-> finite discrete pi_1 sets <-> lcc sheaves on X

Then lambda-local system on X <-> pi_1(X,x_0) f.g. lambda modules


smooth proper basechange theorem. 
Rf^i_*lambda is a lambda local system. if "채워줘" 
